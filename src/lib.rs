mod connection;

pub use connection::Connection;
pub use connection::ConnectionStatus;

/*pub struct File {
    name: String,
    size: usize,
    data: Vector<u8>
}

impl File{
    fn new(name : &str, size: usize, data: &mut[u8]){
        File {name, size, data}
    }
} */
